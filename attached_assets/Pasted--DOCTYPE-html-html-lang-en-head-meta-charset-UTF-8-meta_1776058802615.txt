<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zendar Digital - Look the part. Online.</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --black: #000000;
            --white: #FFFFFF;
            --electric-blue: #0099FF;
            --deep-teal: #005566;
            --light-gray: #D9D9D9;
            --off-white: #F8F8F8;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
            line-height: 1.6;
            color: var(--black);
            background: var(--white);
        }

        /* Header */
        header {
            padding: 2rem 1.5rem;
            background: var(--white);
            border-bottom: 1px solid var(--light-gray);
        }

        .logo-container {
            max-width: 200px;
            margin: 0 auto;
        }

        .logo-container img {
            width: 100%;
            height: auto;
        }

        /* Hero Section */
        .hero {
            padding: 4rem 1.5rem;
            text-align: center;
            background: var(--white);
        }

        .hero h1 {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 1rem;
            letter-spacing: -0.02em;
        }

        .tagline {
            font-size: 1.5rem;
            color: var(--electric-blue);
            font-weight: 600;
            margin-bottom: 2rem;
        }

        .hero-intro {
            font-size: 1.125rem;
            max-width: 600px;
            margin: 0 auto;
            color: var(--deep-teal);
            line-height: 1.8;
        }

        /* Services Section */
        .services {
            padding: 5rem 1.5rem;
            background: var(--off-white);
        }

        .section-title {
            font-size: 2rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 3rem;
            letter-spacing: -0.01em;
        }

        .services-grid {
            display: grid;
            gap: 2rem;
            max-width: 1000px;
            margin: 0 auto;
        }

        .service-card {
            background: var(--white);
            padding: 2.5rem 2rem;
            border-left: 4px solid var(--electric-blue);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .service-card:hover {
            transform: translateX(8px);
            box-shadow: 0 4px 20px rgba(0, 153, 255, 0.15);
        }

        .service-card h3 {
            font-size: 1.375rem;
            margin-bottom: 0.75rem;
            color: var(--black);
            font-weight: 700;
        }

        .service-card p {
            color: var(--deep-teal);
            line-height: 1.7;
        }

        /* Approach Section */
        .approach {
            padding: 5rem 1.5rem;
            background: var(--white);
        }

        .approach-content {
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
        }

        .approach-content p {
            font-size: 1.25rem;
            line-height: 1.9;
            color: var(--deep-teal);
            margin-bottom: 1.5rem;
        }

        /* Contact Section */
        .contact {
            padding: 5rem 1.5rem;
            background: var(--black);
            color: var(--white);
        }

        .contact .section-title {
            color: var(--white);
        }

        .contact-container {
            max-width: 600px;
            margin: 0 auto;
        }

        .whatsapp-btn {
            display: inline-block;
            background: var(--electric-blue);
            color: var(--white);
            padding: 1rem 2.5rem;
            font-size: 1.125rem;
            font-weight: 600;
            text-decoration: none;
            border-radius: 4px;
            margin-bottom: 3rem;
            transition: background 0.2s ease;
        }

        .whatsapp-btn:hover {
            background: var(--deep-teal);
        }

        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group label {
            font-size: 0.875rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: var(--light-gray);
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .form-group input,
        .form-group textarea {
            padding: 1rem;
            font-size: 1rem;
            border: 1px solid var(--light-gray);
            background: var(--white);
            color: var(--black);
            font-family: inherit;
            border-radius: 4px;
        }

        .form-group textarea {
            min-height: 150px;
            resize: vertical;
        }

        .submit-btn {
            background: var(--electric-blue);
            color: var(--white);
            padding: 1rem 2rem;
            font-size: 1.125rem;
            font-weight: 600;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            transition: background 0.2s ease;
        }

        .submit-btn:hover {
            background: var(--deep-teal);
        }

        /* Footer */
        footer {
            padding: 2rem 1.5rem;
            text-align: center;
            background: var(--black);
            color: var(--light-gray);
            font-size: 0.875rem;
            border-top: 1px solid var(--deep-teal);
        }

        /* Tablet and up */
        @media (min-width: 768px) {
            .hero h1 {
                font-size: 3.5rem;
            }

            .tagline {
                font-size: 2rem;
            }

            .services-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .logo-container {
                max-width: 250px;
            }
        }

        /* Desktop */
        @media (min-width: 1024px) {
            .hero {
                padding: 6rem 1.5rem;
            }

            .hero h1 {
                font-size: 4rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <div class="logo-container">
            <img src="ZendarDigital_logo.png" alt="Zendar Digital Logo">
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <h1>Zendar Digital</h1>
        <p class="tagline">Look the part. Online.</p>
        <p class="hero-intro">We help small businesses show up professionally in the digital world.</p>
    </section>

    <!-- Services Section -->
    <section class="services">
        <h2 class="section-title">Services</h2>
        <div class="services-grid">
            <div class="service-card">
                <h3>Website Design</h3>
                <p>Custom websites that make your business look credible and professional from day one.</p>
            </div>
            <div class="service-card">
                <h3>Logo & Branding</h3>
                <p>Distinctive visual identities that help your business stand out and be remembered.</p>
            </div>
            <div class="service-card">
                <h3>Social Media Graphics</h3>
                <p>Professional graphics that keep your social presence consistent and on-brand.</p>
            </div>
            <div class="service-card">
                <h3>Google Business Profile Setup</h3>
                <p>Get found locally with a complete, optimized Google Business Profile that builds trust.</p>
            </div>
        </div>
    </section>

    <!-- Approach Section -->
    <section class="approach">
        <h2 class="section-title">Our Approach</h2>
        <div class="approach-content">
            <p>We work hands-on with small businesses that want to look credible online without the agency price tag. Honest work, clear communication, and designs that actually serve your business goals.</p>
            <p>No fluff. No templates. Just professional digital work that helps you show up with confidence.</p>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact">
        <h2 class="section-title">Get in Touch</h2>
        <div class="contact-container">
            <div style="text-align: center; margin-bottom: 3rem;">
                <a href="https://wa.me/17845262562" class="whatsapp-btn">Message on WhatsApp</a>
            </div>
            
            <form class="contact-form" action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" id="name" name="name" required>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required>
                </div>
                <div class="form-group">
                    <label for="message">Message</label>
                    <textarea id="message" name="message" required></textarea>
                </div>
                <button type="submit" class="submit-btn">Send Message</button>
            </form>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>&copy; 2026 Zendar Digital. All rights reserved.</p>
    </footer>
</body>
</html>